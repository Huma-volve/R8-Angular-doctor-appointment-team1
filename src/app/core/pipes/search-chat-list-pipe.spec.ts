import { SearchChatListPipe } from './search-chat-list-pipe';

describe('SearchChatListPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchChatListPipe();
    expect(pipe).toBeTruthy();
  });
});
